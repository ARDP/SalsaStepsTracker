import { createTheme } from "@mui/material/styles"

const theme = createTheme({
  palette: {
    background: {
      default: "#000",
      paper: "#fff",
    },
    primary: {
      main: "#8C1414",
    },
    secondary: {
      main: "#00bcd4",
    },
  },
})

export default theme
