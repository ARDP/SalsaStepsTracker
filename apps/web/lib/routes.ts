// const API_BASE_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001';

export const ROUTES = {
  home: "/",
  steps: "/steps",
  createStep: "/steps/create",
  login: "/auth/login",
  register: "/auth/register",
}
